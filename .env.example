# Leave FALKORDB_PASSWORD empty for no auth (local dev: the browser login
# then needs only redis://falkordb-server). Set it to enable auth; the same
# value is used by the server and the MCP server.
#   openssl rand -base64 24 | tr -d '/+=' | cut -c1-24
FALKORDB_PASSWORD=
FALKORDB_THREADS=4
